// Generated by generate.py — do not edit. Same-shape filter family.
extern "C" {

int cvx_gen_median_blur(const void* src, int h, int w, int c, void* dst, int ksize)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::medianBlur(s, d, ksize);
    if (d.data != dp) { g_lastError = "median_blur: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_blur(const void* src, int h, int w, int c, void* dst, int ksize)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::blur(s, d, cv::Size(ksize, ksize));
    if (d.data != dp) { g_lastError = "blur: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_bilateral_filter(const void* src, int h, int w, int c, void* dst, int diameter, double sigma_color, double sigma_space)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::bilateralFilter(s, d, diameter, sigma_color, sigma_space);
    if (d.data != dp) { g_lastError = "bilateral_filter: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_equalize_hist(const void* src, int h, int w, int c, void* dst)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::equalizeHist(s, d);
    if (d.data != dp) { g_lastError = "equalize_hist: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_sobel(const void* src, int h, int w, int c, void* dst, int dx, int dy, int ksize)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    { cv::Mat t16; cv::Sobel(s, t16, CV_16S, dx, dy, ksize); cv::convertScaleAbs(t16, d); }
    if (d.data != dp) { g_lastError = "sobel: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_laplacian(const void* src, int h, int w, int c, void* dst, int ksize)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    { cv::Mat t16; cv::Laplacian(s, t16, CV_16S, ksize); cv::convertScaleAbs(t16, d); }
    if (d.data != dp) { g_lastError = "laplacian: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_erode(const void* src, int h, int w, int c, void* dst, int ksize, int iterations, int kshape)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::erode(s, d, cv::getStructuringElement(kshape, cv::Size(ksize, ksize)), cv::Point(-1, -1), iterations);
    if (d.data != dp) { g_lastError = "erode: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_dilate(const void* src, int h, int w, int c, void* dst, int ksize, int iterations, int kshape)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::dilate(s, d, cv::getStructuringElement(kshape, cv::Size(ksize, ksize)), cv::Point(-1, -1), iterations);
    if (d.data != dp) { g_lastError = "dilate: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_morphology(const void* src, int h, int w, int c, void* dst, int op, int ksize, int iterations, int kshape)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::morphologyEx(s, d, op, cv::getStructuringElement(kshape, cv::Size(ksize, ksize)), cv::Point(-1, -1), iterations);
    if (d.data != dp) { g_lastError = "morphology: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

int cvx_gen_flip(const void* src, int h, int w, int c, void* dst, int axis)
{
    CVX_TRY
    cv::Mat s = borrow(src, h, w, c);
    cv::Mat d = borrow(dst, h, w, c);
    void* dp = d.data;
    cv::flip(s, d, axis);
    if (d.data != dp) { g_lastError = "flip: output buffer mismatch"; return -1; }
    return 0;
    CVX_CATCH(-1)
}

} // extern "C"
