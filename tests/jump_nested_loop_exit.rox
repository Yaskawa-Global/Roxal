# 'jump' breaks out of two nested loops to a label after them.
var found = "none"
scope:
    for i in [0, 1, 2]:
        for j in [0, 1, 2]:
            if i == 1 and j == 2:
                found = "{i},{j}"
                jump done
    label done
print(found)
print("after")
