
type E event

type MyActor actor:

  proc setup():
    on E:
      print('e handled in MyActor')

  proc trigger():
    emit E()


a = MyActor()

wait(us=1000)

a.trigger() // setup not called, so should be no hander

wait(us=1000)

a = nil

