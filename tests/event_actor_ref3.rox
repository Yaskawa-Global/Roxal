
type MyActor actor:

  private var e :event

  proc setup():
    on e:
      print('e handled in MyActor')

  proc trigger():
    emit e


a = MyActor()

sleep(us=1000)

a.trigger() // setup not called, so should be no hander

sleep(us=1000)

a = nil

