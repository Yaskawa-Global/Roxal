type Outer object:
    var x: int
    type Inner object:
        var y: int

# construct nested type via enclosing type
var inner = Outer.Inner(99)
print(inner.y)

# nested type is NOT accessible unqualified
var inner2 = Outer.Inner(7)
print(inner2.y)
