// Case 3: const return + sole-owner → frozen snapshot via createFrozenSnapshot
// The returned value should be const — mutation errors.

type Worker actor:
    func make_list() -> const list:
        var l = [1, 2, 3]
        return l

var w = Worker()
var result = w.make_list()
print(result)
result[0] = 99
