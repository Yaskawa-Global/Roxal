import roxal_examples.*

var svc = EverythingService("__GRPC_TEST_ADDR__", { 'timeout_ms': 2000 })

// Empty request type: no args (auto-constructed)
print(svc.Ping())

// Empty request type: explicit request object (backwards compat)
print(svc.Ping(PingRequest()))
